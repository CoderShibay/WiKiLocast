package com.wikifm.ui.theme

import androidx.compose.ui.graphics.Color

// Background — near-black with subtle depth (Spotify-style)
val BgDeep    = Color(0xFF0A0A0F)
val BgMid     = Color(0xFF111118)
val BgSurface = Color(0xFF1A1A24)

// Glass cards on dark background
val GlassSurface   = Color(0x1AFFFFFF)   // 10% white
val GlassSurfaceHi = Color(0x26FFFFFF)   // 15% white
val GlassBorder    = Color(0x33FFFFFF)   // 20% white
val GlassBorderDim = Color(0x18FFFFFF)   // 9% white

// Text — high contrast, actually readable
val TextPrimary   = Color(0xFFFFFFFF)    // pure white
val TextSecondary = Color(0xCCFFFFFF)    // 80% white
val TextMuted     = Color(0x80FFFFFF)    // 50% white

// Accents
val AccentAmber     = Color(0xFFFFB300)
val AccentAmberDeep = Color(0xFFE65C00)
val AccentGreen     = Color(0xFF1DB954)  // Spotify green for ON AIR
val AccentRed       = Color(0xFFFF4444)
